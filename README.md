# JSON-VALIDATOR-FORMATTER
I built a JSON validator and formatter that checks syntax and highlights errors, then formats valid JSON into clean, readable structure using JSON.parse() and JSON.stringify() in a simple browser interface.
